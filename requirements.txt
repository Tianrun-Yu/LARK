# Core deep-learning stack
torch>=2.4
transformers>=4.45
accelerate>=0.30
deepspeed>=0.14
peft>=0.12
liger-kernel>=0.5
flash-attn>=2.5            # optional but strongly recommended on Ampere/Hopper

# Inference (used by lark/eval/generate.py)
vllm>=0.6                  # primary; sglang is an alternative engine
sglang>=0.4                # optional fallback engine

# Numerics / utils
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4
pyyaml>=6.0
tqdm>=4.66

# Plotting (only required to reproduce the appendix figures)
matplotlib>=3.8
